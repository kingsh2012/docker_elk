input {
	tcp {
		port => 5000
	}
}

## Add your filters / logstash plugins configuration here

output {
	elasticsearch {
		hosts => "10.0.1.131:9200"
		user => "elastic"
		password => "1qaz2wsx"
	}
}
